
name: Build Kivy APK
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            python3-pip \
            python3-setuptools \
            python3-wheel \
            git \
            zip \
            unzip \
            wget \
            openjdk-11-jdk \
            build-essential \
            libssl-dev \
            libffi-dev \
            python3-dev \
            libltdl-dev
      
      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install buildozer cython
      
      - name: Setup Android SDK manually with correct structure
        run: |
          # Set up directories
          export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
          mkdir -p $ANDROID_HOME
          
          # Download command line tools
          cd /tmp
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip
          
          # Create correct directory structure
          mkdir -p $ANDROID_HOME/cmdline-tools/latest
          mv cmdline-tools/* $ANDROID_HOME/cmdline-tools/latest/
          
          # Create legacy tools directory for Buildozer
          mkdir -p $ANDROID_HOME/tools/bin
          cp $ANDROID_HOME/cmdline-tools/latest/bin/* $ANDROID_HOME/tools/bin/
          
          # Set permissions
          chmod +x $ANDROID_HOME/cmdline-tools/latest/bin/*
          chmod +x $ANDROID_HOME/tools/bin/*
          
          # Set environment
          export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
          
          # Accept licenses with explicit SDK root
          yes | sdkmanager --sdk_root=$ANDROID_HOME --licenses
          
          # Install required packages
          sdkmanager --sdk_root=$ANDROID_HOME "platform-tools"
          sdkmanager --sdk_root=$ANDROID_HOME "platforms;android-33"
          sdkmanager --sdk_root=$ANDROID_HOME "build-tools;33.0.2"
          
          # Download and install NDK manually
          cd /tmp
          wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip -O ndk.zip
          unzip -q ndk.zip
          # Ensure NDK directory exists before moving
          mkdir -p $ANDROID_HOME/ndk
          mv android-ndk-r25c $ANDROID_HOME/ndk/25.2.9519653
          
          echo "Android SDK setup complete"
          ls -la $ANDROID_HOME/
      
      - name: Cache buildozer global directory
        uses: actions/cache@v4
        with:
          path: ~/.buildozer
          key: buildozer-${{ hashFiles('**/.buildozer') }}
      
      - name: Build APK
        run: |
          export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
          export ANDROID_SDK_ROOT=$HOME/.buildozer/android/platform/android-sdk
          export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
          export PATH=$ANDROID_HOME/platform-tools:$ANDROID_HOME/tools/bin:$JAVA_HOME/bin:$PATH
          
          # Initialize buildozer if buildozer.spec doesn't exist
          if [ ! -f "buildozer.spec" ]; then
            buildozer init
          fi
          
          # Modify buildozer.spec to use correct paths if it exists
          if [ -f "buildozer.spec" ]; then
            # Ensure correct Android API and NDK versions are set
            sed -i 's/^#android.api = .*/android.api = 33/' buildozer.spec
            sed -i 's/^android.api = .*/android.api = 33/' buildozer.spec
            sed -i 's/^#android.ndk = .*/android.ndk = 25.2.9519653/' buildozer.spec
            sed -i 's/^android.ndk = .*/android.ndk = 25.2.9519653/' buildozer.spec
            sed -i 's/^#android.sdk = .*/android.sdk = 33/' buildozer.spec
            sed -i 's/^android.sdk = .*/android.sdk = 33/' buildozer.spec
          fi
          
          # Build the APK
          buildozer -v android debug
      
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: kivy-apk-${{ github.sha }}
          path: bin/*.apk
          retention-days: 30
      
      - name: Upload build logs on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: build-logs-${{ github.sha }}
          path: |
            .buildozer/
            *.log
          retention-days: 7
